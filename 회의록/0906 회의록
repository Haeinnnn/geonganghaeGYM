9/6일 화요일 온라인 팀회의 (https://meet.jit.si/)

csv로 값을 저장하는 코드

현재 round함수가 제대로 작동하지 않는 문제가 있음
(171이면 /10 시켜서 17로 나와야하는데 171로 나옴)
일단 csv로 저장되는 코드 올리기 (혜은, 가은)


# 각도를 어떻게 학습시키는지 #

학습을 시키는 사이클은 모르겠지만
처음에 물어본건 딕셔너리였으니까 현재 csv로 진행할거면
값 자체는 원래값으로 받고 학습시킬때 round써서 처리하는걸로 하는게 맞는거 같다
제각각 낸걸로 학습시킨다음에 마지막 출력값만 round

학습시키는건 k-means를 써보기
k-means 비지도학습/군집화
케이민즈는 일단 예시로 든 방식으로
군집화를 하면 오차가 보완될거 같고 오차를 활용할 수 있을것으로 보임
k-means를 할 때 x y 값을 기본 x,y로 넣지말고 각도로 해야함
(사진을 가까이서 찍은 시점의 x,y값과 멀리서 찍은 x,y값이 다름)
일단 소수점을 다 쓰는게 아닌 2~3번째자리까지만 쓰고 k-means를 진행해보기


# x y 값을 어떻게 해야 하는지 #

x y 어떻게 정할건지
하려고 하는게 앵글이 2개니까
라벨링은 csv로 전부 다 뽑아놓고 입력을 어떻게 해야할지 고민해보기


# draw_result 랑 extract_and_draw의 차이 #

draw_result 랑 extract_and_draw 가 겹치는게 많은데
강사님이 짠 코드랑, 원래 openpose의 코드로 추측중

질문할때는 1번을 해결하고 2번을 질문하자
csv가 이렇게 나왔는데 이걸 학습을 시키려고 했는데
지금 생각해보니까 학습 방법을 잘 모르겠다
우선은 x와 y값을 ~할건데
모델을 어떻게 할건데 조언좀 부탁드립니다~
+ 1번해결하고 바로 메일드리기, 맨 위의 round 코드 오류나는것도 질문드리기

---------------------------------------------------------------

내일 회의 전까지 순서대로 (6시)

- x y 값을 어떻게 해야 하는지
- 각도를 어떻게 학습시키는지
- draw_result 랑 extract_and_draw
